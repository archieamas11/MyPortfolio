{
"scripts": {
  "minify": "minify --auto index.html -o index.html && minify --auto assets/css -o assets/css && minify --auto assets/js -o assets/js",
  "build": "npm run minify"
  
},
  "devDependencies": {
    "minify": "^14.0.0"
  }
}
